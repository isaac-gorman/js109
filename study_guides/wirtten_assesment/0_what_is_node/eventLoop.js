// The Event Loop:

// Q: What is the event loop?
// A: The event loop is what handles asynchronous operations.
// A: JavaScript is a single threaded language (thread is the thing that actually executes your instructions) so the JS engine has only one call stack thus enabling it to only do one thing at a time.

// A: By default the JS engine executes code from top to bottom, line by line. In other words synchronous. So the event loop is what enables it to actually execute code asynchronously.

// Q: What will I learn form this annotation?

// JSs' single-threaded model:

// Q: How many threads does JS have?

// Q: How does the JS engine execute js files?

// Q:
